import { NameToImagePipe } from './name-to-image.pipe';

describe('NameToImagePipe', () => {
  it('create an instance', () => {
    const pipe = new NameToImagePipe();
    expect(pipe).toBeTruthy();
  });
});
